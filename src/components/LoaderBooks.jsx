import React from 'react';
import { Loader } from 'semantic-ui-react';

const LoaderExampleInlineCentered = () => (
    <Loader active content="Загрузка книг" inline="centered" />
);

export default LoaderExampleInlineCentered;
